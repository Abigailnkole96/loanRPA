pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // Create a virtual environment and install dependencies
                sh '''
                    python3 -m venv venv
                    . venv/bin/activate
                    pip install -r app/requirements.txt
                '''
            }
        }
        stage('Test') {
            steps {
                // Activate the virtual environment and run tests
                sh '''
                    . venv/bin/activate
                    echo "Running tests"
                    python -m unittest discover || { echo "Unittest failed, trying pytest..."; pytest --disable-warnings; }
                '''
            }
        }
        stage('Deploy') {
            steps {
                // Activate the virtual environment before deploying
                sh '''
                    . venv/bin/activate
                    terraform -chdir=infrastructure/ apply -auto-approve
                '''
            }
        }
    }
}
